name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.1.1'
      SERVERPOD_PASSWORD_database: 'VFp7jgUSXokws2czjXaZX2_aFgrDalpB'
      SERVERPOD_PASSWORD_redis: '-IdvzYHyTxggAT2DbqWeLV0OJ9URLVsp'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: student_assistant_server
        run: docker compose up --build -d

      - name: Install dependencies
        working-directory: student_assistant_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: student_assistant_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: student_assistant_server
        run: serverpod generate

      - name: Run tests
        working-directory: student_assistant_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: student_assistant_server
        run: docker compose down -v
